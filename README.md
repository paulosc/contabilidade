# Contabilidade

Projeto em montagem.

- Repositório: https://github.com/paulosc/contabilidade
- Firebase: `contabilidade-a9d35`
- Região pretendida: `southamerica-east1`

## Situação

Ainda não há aplicação aqui. O que já está definido:

- o alias do Firebase (`.firebaserc`) aponta para `contabilidade-a9d35`;
- o `.gitignore` cobre a stack pretendida (Vite + Cloud Functions).

## Pendente para o primeiro deploy

1. Definir o escopo da aplicação.
2. Provisionar o projeto Firebase (plano Blaze, Firestore, Authentication, Storage).
3. Registrar um app web e preencher `web/.env.local`.
